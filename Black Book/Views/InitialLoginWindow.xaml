<!-- InitialLoginWindow.xaml -->
<Window x:Class="BlackBook.Views.InitialLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Open Profile"
        Height="174" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" FontFamily="Verdana">
    <Grid Margin="20,20,20,0" Height="110" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <!-- Label column -->
            <ColumnDefinition Width="*"/>
            <!-- Input field column -->
        </Grid.ColumnDefinitions>

        <!-- Profile selection -->
        <TextBlock Text="Profile:" VerticalAlignment="Center" 
                   Grid.Row="0" Grid.Column="0" Margin="0 0 8 0"/>
        <ComboBox x:Name="ProfileList" Grid.Row="0" Grid.Column="1" 
                  Margin="0,3,0,9"
                  MinWidth="200"
                  SelectionChanged="ProfileList_SelectionChanged"/>

        <!-- Password input -->
        <TextBlock Text="Password:" VerticalAlignment="Center" Grid.Column="0" Margin="-2,0,11,0" Height="20" Grid.Row="2"/>
        <PasswordBox x:Name="PasswordInput" Grid.Row="1" Grid.Column="1" 
                     MinWidth="200" Grid.RowSpan="2" Margin="0,0,0,3"/>

        <!-- Action buttons -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right"/>
        <Button Content="Cancel" Margin="22,35,238,-45" 
                    Click="Cancel_Click" 
                    IsCancel="True" Grid.ColumnSpan="2" Grid.Row="2"/>
        <Button Content="Unlock" Margin="40,35,150,-45"
                    Click="Unlock_Click" 
                    IsDefault="True" Grid.Column="1" Grid.Row="2"/>
        <Button Content="Create New Profile"
                    Click="CreateProfile_Click" Grid.Column="1" Margin="128,35,2,-45" Grid.Row="2"/>
    </Grid>
</Window>
