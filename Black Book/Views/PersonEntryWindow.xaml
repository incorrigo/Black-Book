<Window x:Class="BlackBook.Views.PersonEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Person"
        Height="360" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name:"       Grid.Row="0" Grid.Column="0" Margin="0 4"/>
        <TextBlock Text="Email:"      Grid.Row="1" Grid.Column="0" Margin="0 4"/>
        <TextBlock Text="Phone:"      Grid.Row="2" Grid.Column="0" Margin="0 4"/>
        <TextBlock Text="Company:"    Grid.Row="3" Grid.Column="0" Margin="0 4"/>
        <TextBlock Text="Relation:"   Grid.Row="4" Grid.Column="0" Margin="0 4"/>

        <TextBox    x:Name="NameBox"     Grid.Row="0" Grid.Column="1" Margin="0 2" />
        <TextBox    x:Name="EmailBox"    Grid.Row="1" Grid.Column="1" Margin="0 2" />
        <TextBox    x:Name="PhoneBox"    Grid.Row="2" Grid.Column="1" Margin="0 2" />

        <!-- allow free text or pick existing company -->
        <ComboBox   x:Name="CompanyBox"  Grid.Row="3" Grid.Column="1" Margin="0 2"
                    IsEditable="True"
                    ItemsSource="{Binding Companies}"
                    DisplayMemberPath="Name"
                    StaysOpenOnEdit="True"/>

        <ComboBox   x:Name="RelationBox" Grid.Row="4" Grid.Column="1" Margin="0 2"
                    SelectedIndex="0">
            <ComboBoxItem Content="Unknown"/>
            <ComboBoxItem Content="Friendly"/>
            <ComboBoxItem Content="Neutral"/>
            <ComboBoxItem Content="Adversarial"/>
        </ComboBox>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0 20 0 0">
            <Button Content="Cancel" Width="88" Margin="0 0 8 0"
                    Click="Cancel_Click"/>
            <Button Content="Save"   Width="88"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
